# Plugin metadata — consumed by build.gradle.kts via providers.gradleProperty(...)
pluginGroup = me.heloworld
pluginVersion = 0.1.0
# Minimum compatible IDE build; untilBuild is left open in build.gradle.kts.
pluginSinceBuild = 243

# Target IntelliJ Platform (IC = IntelliJ IDEA Community; works in Ultimate too).
platformType = IC
platformVersion = 2024.3

org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.caching=true
org.gradle.configuration-cache=true
kotlin.stdlib.default.dependency=false
